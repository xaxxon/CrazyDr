
// responsible for adding the form template to each section of action
// once when the page is loaded and then each time the lowest one
// is entered in so the user can specify more
$('document').ready( function(){
    alert("HI");                     
})